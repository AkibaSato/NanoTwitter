<form id="my-form4-<%= tweets2[i].id %>">
  <button id="unlike_ajax" class="btn-default" type="submit" value="Unlike"><p4 id="unlike-<%= tweets2[i].id %>">Unlike</p4></button>
</form>



<script>
    $('#my-form4-<%= tweets2[i].id %>').submit(function(event) {
        event.preventDefault();

        var $form = $(this), url = "/tweets/<%= tweets2[i].id %>/unlike";
        var posting = $.post( url );

        posting.done(function( data ) {
          $("#unlike-<%= tweets2[i].id %>").text('Unliked');
          $("#like-<%= tweets2[i].id %>").text('Like');
        });
    });
</script>
