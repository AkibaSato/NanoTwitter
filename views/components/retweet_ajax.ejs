<form id="my-form5-<%= tweets[i].id %>" method="post">
  <input type="hidden" id="content" name="content" value="<%= tweets[i].content %>">
  <button id="retweet_ajax" class="btn-default" type="submit" value="Retweet"><p5 id="retweet-<%= tweets[i].id %>">Retweet</p3></button>
</form>






<script>
    $('#my-form5-<%= tweets[i].id %>').submit(function(event) {
        event.preventDefault();

        var $form = $(this), url = "/tweets/<%= tweets[i].id %>/retweet";
        var posting = $.post( url, { content: $('#content').val() } );

        posting.done(function( data ) {
          $("#retweet-<%= tweets[i].id %>").text('Retweeted');
          console.log('Retweeted');
        });
    });
</script>
