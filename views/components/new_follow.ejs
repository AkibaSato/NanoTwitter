<form method="post" action="/api/v1/<%= API_TOKEN %>/user/<%= user.id %>/follow">
  <div class="box">
    <% if (me) { %>
      <button class="btn new-tweet__button" type="submit" style="float: right;">Follow</button>
      <% } %>
  </div>
</form>
