<form id="my-form3-<%= tweets[i].id %>">
  <button id="new_like_ajax" class="btn-default" type="submit" value="Like"><p3 id="like-<%= tweets[i].id %>">Like</p3></button>
</form>

<script>
    $('#my-form3-<%= tweets[i].id %>').submit(function(event) {
        event.preventDefault();

        var $form = $(this), url = "/api/v1/<%= API_TOKEN %>/tweets/<%= tweets[i].id %>/like";
        var posting = $.post( url );

        posting.done(function( data ) {
          $("#like-<%= tweets[i].id %>").text('Liked');
          $("#unlike-<%= tweets[i].id %>").text('Unlike');
        });
    });
</script>
